package sirgl.simple.vm.roots

import kotlin.coroutines.experimental.buildSequence

class SourceRootScanner {
    fun findSources(sourceRoot: SourceRoot): Sequence<FileSymbolSource> {
        return buildSequence {
            TODO()
        }
    }
}