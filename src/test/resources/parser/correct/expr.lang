package p;

class A {
    fun foo(p1: I32) {
        1 + 2 + 3; // associativity
        1 * 2 + 3 * 4; // precedence
        x = y = z = 100; // assign
        foo.bar.baz = 12; // qualified
        foo.bar(); // simple call
        foo.bar.baz(12, "test"); // arguments
        baz(12); // single
        super.field; // field
        super.call(); // call
        super(); // constructor
        this.foo();
        this.field;
        this;
        this.super.foo.this.super.bar = 12;
        (cls.field + 12) * 42;
        field.super().arr[12];
        (foo as I32) is I8;
        null.foo();
    }
}