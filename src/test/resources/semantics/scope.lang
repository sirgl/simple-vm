class A {
    fun foo() {
        var y: i32 = 12;
        var y: i32 = 12; // redeclaration
    }
    fun bar() {
        var bar: i32 = 0; // ok
    }

    fun foo() { // redeclaration

    }

    fun duplicatingParams(x: i32, x: i32) {
    }

    var field: i32 = 2;
    var field: i32 = 244;
}